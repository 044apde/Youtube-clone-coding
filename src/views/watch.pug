extends base.pug

block content 
    p=video.description
    p 
        small=video.createdAt
    a(href=`${video.id}/edit`) Edit Video &uarr; 
    br 
    a(href=`${video.id}/delete`) Delete Video &uarr; 